version: 0.2

phases:
  # install:
  #   commands:
  #     - echo Installing Node 12...
  #     - npm install npm -g && node -v && npm -v
  #     - npm install -g @vue/cli
  pre_build:
    commands:
      - npm install
  build:
    commands:
      - npm run build
  post_build:
    commands:
      - echo nothing to do
      - zip -r dist.zip dist
artifacts:
  # files:
  #   - '**/*'
  files:
    - dist.zip
    - appspec.yml
  # base-directory: dist
